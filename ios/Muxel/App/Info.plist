<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleDisplayName</key>
	<string>muxel</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Allow the terminal's CADisplayLink renderer to run above 60 Hz on ProMotion
	     devices (otherwise iOS caps it at 60), for smoother scrolling/redraw. -->
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchBackground</string>
	</dict>
	<!-- Live Activity (the backgrounded status bar on Lock Screen / Dynamic Island). -->
	<key>NSSupportsLiveActivities</key>
	<true/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>

	<!-- Background execution: BGAppRefreshTask-driven status polling. Only the
	     app-refresh ("fetch") mode is used — the poll runs as a BGAppRefreshTaskRequest
	     (see AppDelegate). Do NOT add "processing" here unless a BGProcessingTaskRequest
	     is actually registered and submitted: declaring an unused background mode is an
	     App Review 2.5.4 rejection. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>fetch</string>
	</array>
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>dev.muxel.ios.poll</string>
	</array>

	<!-- Face ID gate for revealing stored SSH credentials. -->
	<key>NSFaceIDUsageDescription</key>
	<string>muxel uses Face ID to protect your stored SSH passwords and keys.</string>

	<!-- Export compliance. muxel bundles an SSH client (SwiftNIO SSH via Citadel), so
	     it uses non-exempt encryption (not just Apple's HTTPS/OS crypto) → this is YES.
	     It qualifies for the mass-market exemption (standard algorithms only, no
	     proprietary crypto), which requires a one-time-per-year self-classification
	     report to BIS + Apple's export questionnaire — see AppStore/Submission.md.
	     Declaring the key here lets App Store Connect skip the per-build prompt. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<true/>
</dict>
</plist>
